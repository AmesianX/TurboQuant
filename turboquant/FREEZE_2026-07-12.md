# ❄️ PROJECT FREEZE — 2026-07-12 (clean checkpoint, NOT a failure exit)

Frozen by owner decision (Fable 5 goes paid; resume when economics change).
**This is the best handoff state this repo has ever been in.** Everything below is
committed on `feat/dsv4-w4a16-native-port`; production serving = vLLM DSpark (untouched).

## Scoreboard vs target (prefill ~1600± / decode ~57±, vLLM-parity dream)
| metric | frozen at | target | note |
|---|---|---|---|
| prefill 13k | **326 t/s** @ub2048+EP (completes; was OOM) | ~1600± | gap ×4.9, fully mapped below |
| decode | **13.0 t/s** (graphs+F8+MTP) | ~57± | +30% this session from 10.0 |

## What this session proved/landed (commits fec580785..056de1f6e)
1. **Decode +30%**: EP decode CUDA graphs (deadlock myth busted), lm_head F8 load-convert,
   MTP composition. `DSV4_EP_DECODE_GRAPH=1 DSV4_LM_HEAD_F8=1`.
2. **MLA attention TP head-split CORRECT on 2-node** (41794b115, `DSV4_ATTN_SPLIT=1`,
   default OFF): overturns "MLA can't head-split". Net-negative until whole-step NCCL
   graph capture (boundary cost 43→86 × ~0.2ms > 11.7ms read savings).
3. **Prefill Wall B killed** (e7246adf0/e08dc1be6, `DSV4_MOE_FC1_FUSED=1`): fc1-fused load
   layout = zero-copy repack alias + EP dead-SFB skip (8.6GB) + simple-SF free at repack
   (8.6GB). 13k completes. Decode verified correct pre/post repack.
4. **The 1600 enemy list, measured** (056de1f6e, DSV4_OPPROF): FA 23.2% (ALREADY MMA —
   kernel-level work), CONT 13.2%, glue ~15%, f8 dense GEMM ~11% @28TF/s, wo_a mul_mat_id
   8.2%, MoE 8.3% (DONE — 43.7TF/s), wall-gap 38% (eager). MoE/ub-climbing = dead levers.

## Resume order (each step has a numeric gate; kill if gates fail)
1. CONT + elementwise glue fusion (~25% pool, cheapest) → expect ~420-480.
2. wo_a as per-group GEMM — permute to [gd, T, G] (NOT [gd,1,G,T]: measured 3× SLOWER).
3. f8 dense GEMM → native FP8 GEMM path (feat/dsv4-fp8-native-gemm asset) → ~550-700?
4. FA D=512 kernel deep work (tile/sparse-mask cost) — the 23.2%.
5. Whole-step CUDA graph incl. NCCL (kills 38% wall gap AND flips ATTN_SPLIT positive
   → decode path to 45+). This is the shared decode/prefill endgame lever.

## Traps documented (do not re-step)
- KERNEL_PROF hangs at init with FC1_FUSED on 2-node SPMD (×2 repro) — use DSV4_OPPROF
  (+ GGML_CUDA_NO_GRAPHS=1 for full coverage).
- nsys 2024.6 captures no CUDA kernel data on GB10 here; TMPDIR must be user-writable.
- ub>2048 OOMs at init reserve on THIS stack (73GB EP sidecar resident); steady compute
  buffer is only ~1MB/token — the reserve overshoot was never named (moot: ub is dead).
- MTP needs UB=256 (memwatch); bench servers: --ctx-checkpoints 0, CACHE_RAM=0.

## Launch (experiment stack, tp-serve/tp-w4a16.sh — tp.sh untouched)
Decode-best: `PORT=8081 CTX=262144 UB=256 DSV4_EP=1 DSV4_MOE_GROUPED=1 DSV4_MOE_FUSED=1
DSV4_MOE_FC1_FUSED=1 DSV4_MOE_SIDECAR=~/Models/DeepSeek-V4-Flash-GGUF/FP4/nvfp4_sidecar_ep
DSV4_EP_DECODE_GRAPH=1 DSV4_LM_HEAD_F8=1 bash tp-serve/tp-w4a16.sh ALLRESTART`
Prefill-bench: same + `CTX=131072 UB=2048 SPEC="" DSV4_INDEXER_QTILE=2048
EXTRA_ARGS="--ctx-checkpoints 0" CACHE_RAM=0`; 13k body: session scratchpad prefill13k.json
(regenerate: any ~60KB text + max_tokens 8).

Full context: DSV4_ATTN_TP_SPLIT_DESIGN.md (this dir), AIDEN_1600_STACK.md,
DSV4_PREFILL_GAP_ROUND1-6. 밤새 여기까지. 꿈은 썩지 않는다 — 지도는 전부 여기 있다.
